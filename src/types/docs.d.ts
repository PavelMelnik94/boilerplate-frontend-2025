/**
 * @category Components
 * @component
 * @example
 * ```tsx
 * <MyComponent prop="value" />
 * ```
 */

/**
 * @category Hooks
 * @hook
 * @example
 * ```tsx
 * const result = useMyHook();
 * ```
 */

/**
 * @category Utils
 * @function
 * @example
 * ```ts
 * const result = myUtilFunction(param);
 * ```
 */

/**
 * @category Types
 * @typedef {Object} MyType
 */
